import 'package:flutter/material.dart';

class ProviderJobFeedScreen extends StatelessWidget {
  const ProviderJobFeedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Provider Job Feed')),
      body: const Center(child: Text('Provider job feed placeholder')),
    );
  }
}
